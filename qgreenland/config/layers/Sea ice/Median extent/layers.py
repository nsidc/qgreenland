# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


seaice_median_extent_01 = ConfigLayer(
    id='seaice_median_extent_01',
    title='January',
    description=(
        """Ice edge position line that is typical for a month, based on median
        extent from the period 1981 through 2010."""
    ),
    tags=[],
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['median_extent_line_01'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


seaice_median_extent_02 = ConfigLayer(
    id='seaice_median_extent_02',
    title='February',
    description=(
        """Ice edge position line that is typical for a month, based on median
        extent from the period 1981 through 2010."""
    ),
    tags=[],
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['median_extent_line_02'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


seaice_median_extent_03 = ConfigLayer(
    id='seaice_median_extent_03',
    title='March',
    description=(
        """Ice edge position line that is typical for a month, based on median
        extent from the period 1981 through 2010."""
    ),
    tags=[],
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['median_extent_line_03'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


seaice_median_extent_04 = ConfigLayer(
    id='seaice_median_extent_04',
    title='April',
    description=(
        """Ice edge position line that is typical for a month, based on median
        extent from the period 1981 through 2010."""
    ),
    tags=[],
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['median_extent_line_04'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


seaice_median_extent_05 = ConfigLayer(
    id='seaice_median_extent_05',
    title='May',
    description=(
        """Ice edge position line that is typical for a month, based on median
        extent from the period 1981 through 2010."""
    ),
    tags=[],
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['median_extent_line_05'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


seaice_median_extent_06 = ConfigLayer(
    id='seaice_median_extent_06',
    title='June',
    description=(
        """Ice edge position line that is typical for a month, based on median
        extent from the period 1981 through 2010."""
    ),
    tags=[],
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['median_extent_line_06'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


seaice_median_extent_07 = ConfigLayer(
    id='seaice_median_extent_07',
    title='July',
    description=(
        """Ice edge position line that is typical for a month, based on median
        extent from the period 1981 through 2010."""
    ),
    tags=[],
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['median_extent_line_07'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


seaice_median_extent_08 = ConfigLayer(
    id='seaice_median_extent_08',
    title='August',
    description=(
        """Ice edge position line that is typical for a month, based on median
        extent from the period 1981 through 2010."""
    ),
    tags=[],
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['median_extent_line_08'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


seaice_median_extent_09 = ConfigLayer(
    id='seaice_median_extent_09',
    title='September',
    description=(
        """Ice edge position line that is typical for a month, based on median
        extent from the period 1981 through 2010."""
    ),
    tags=[],
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['median_extent_line_09'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


seaice_median_extent_10 = ConfigLayer(
    id='seaice_median_extent_10',
    title='October',
    description=(
        """Ice edge position line that is typical for a month, based on median
        extent from the period 1981 through 2010."""
    ),
    tags=[],
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['median_extent_line_10'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


seaice_median_extent_11 = ConfigLayer(
    id='seaice_median_extent_11',
    title='November',
    description=(
        """Ice edge position line that is typical for a month, based on median
        extent from the period 1981 through 2010."""
    ),
    tags=[],
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['median_extent_line_11'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


seaice_median_extent_12 = ConfigLayer(
    id='seaice_median_extent_12',
    title='December',
    description=(
        """Ice edge position line that is typical for a month, based on median
        extent from the period 1981 through 2010."""
    ),
    tags=[],
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['median_extent_line_12'],
    ),
    steps=[
        # TODO
    ],
)
