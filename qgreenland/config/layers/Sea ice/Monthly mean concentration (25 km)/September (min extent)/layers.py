# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


minimum_concentration_2010 = ConfigLayer(
    id='minimum_concentration_2010',
    title='September 2010',
    description=(
        """Monthly average of sea ice concentration as a percentage (e.g., 99.9
        = 99.9%). Values under 15% are considered to be open water."""
    ),
    tags=[],
    style='sea_ice_concentration',
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['minimum_concentration_2010'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


minimum_concentration_2011 = ConfigLayer(
    id='minimum_concentration_2011',
    title='September 2011',
    description=(
        """Monthly average of sea ice concentration as a percentage (e.g., 99.9
        = 99.9%). Values under 15% are considered to be open water."""
    ),
    tags=[],
    style='sea_ice_concentration',
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['minimum_concentration_2011'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


minimum_concentration_2012 = ConfigLayer(
    id='minimum_concentration_2012',
    title='September 2012',
    description=(
        """Monthly average of sea ice concentration as a percentage (e.g., 99.9
        = 99.9%). Values under 15% are considered to be open water."""
    ),
    tags=[],
    style='sea_ice_concentration',
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['minimum_concentration_2012'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


minimum_concentration_2013 = ConfigLayer(
    id='minimum_concentration_2013',
    title='September 2013',
    description=(
        """Monthly average of sea ice concentration as a percentage (e.g., 99.9
        = 99.9%). Values under 15% are considered to be open water."""
    ),
    tags=[],
    style='sea_ice_concentration',
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['minimum_concentration_2013'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


minimum_concentration_2014 = ConfigLayer(
    id='minimum_concentration_2014',
    title='September 2014',
    description=(
        """Monthly average of sea ice concentration as a percentage (e.g., 99.9
        = 99.9%). Values under 15% are considered to be open water."""
    ),
    tags=[],
    style='sea_ice_concentration',
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['minimum_concentration_2014'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


minimum_concentration_2015 = ConfigLayer(
    id='minimum_concentration_2015',
    title='September 2015',
    description=(
        """Monthly average of sea ice concentration as a percentage (e.g., 99.9
        = 99.9%). Values under 15% are considered to be open water."""
    ),
    tags=[],
    style='sea_ice_concentration',
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['minimum_concentration_2015'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


minimum_concentration_2016 = ConfigLayer(
    id='minimum_concentration_2016',
    title='September 2016',
    description=(
        """Monthly average of sea ice concentration as a percentage (e.g., 99.9
        = 99.9%). Values under 15% are considered to be open water."""
    ),
    tags=[],
    style='sea_ice_concentration',
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['minimum_concentration_2016'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


minimum_concentration_2017 = ConfigLayer(
    id='minimum_concentration_2017',
    title='September 2017',
    description=(
        """Monthly average of sea ice concentration as a percentage (e.g., 99.9
        = 99.9%). Values under 15% are considered to be open water."""
    ),
    tags=[],
    style='sea_ice_concentration',
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['minimum_concentration_2017'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


minimum_concentration_2018 = ConfigLayer(
    id='minimum_concentration_2018',
    title='September 2018',
    description=(
        """Monthly average of sea ice concentration as a percentage (e.g., 99.9
        = 99.9%). Values under 15% are considered to be open water."""
    ),
    tags=[],
    style='sea_ice_concentration',
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['minimum_concentration_2018'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


minimum_concentration_2019 = ConfigLayer(
    id='minimum_concentration_2019',
    title='September 2019',
    description=(
        """Monthly average of sea ice concentration as a percentage (e.g., 99.9
        = 99.9%). Values under 15% are considered to be open water."""
    ),
    tags=[],
    style='sea_ice_concentration',
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['minimum_concentration_2019'],
    ),
    steps=[
        # TODO
    ],
)
# TODO
from qgreenland.config.datasets.XXXX import XXXX as dataset
from qgreenland.models.config.layer import ConfigLayer, ConfigLayerInput


minimum_concentration_2020 = ConfigLayer(
    id='minimum_concentration_2020',
    title='September 2020',
    description=(
        """Monthly average of sea ice concentration as a percentage (e.g., 99.9
        = 99.9%). Values under 15% are considered to be open water."""
    ),
    tags=[],
    style='sea_ice_concentration',
    input=ConfigLayerInput(
        dataset=dataset,
        asset=dataset.assets['minimum_concentration_2020'],
    ),
    steps=[
        # TODO
    ],
)
